# Persisted Intent Packs — runtime data, never commit
data/

# Dependencies
node_modules/

# Build output
dist/

# Environment files — never commit real secrets
.env
.env.local
.env.*.local
# .env.example is committed intentionally (no secrets, only placeholders)
!.env.example
